#include<stdio.h>

int main() 
{
    int a,b,sum;
    printf("Enter two number for Addition\n");
    scanf("%d%d",  &a, &b);
    sum=a+b;
    printf("The Sum of %d + %d = %d", a, b, sum);
    return 0;
}
